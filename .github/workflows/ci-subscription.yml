name: CI - subscription

on:
  push:
    branches: [main]
    paths:
      - 'services/subscription/**'
      - '.github/workflows/ci-subscription.yml'
  pull_request:
    branches: [main]
    paths:
      - 'services/subscription/**'
      - '.github/workflows/ci-subscription.yml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/subscription

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/subscription
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: services/subscription/go.sum
      - name: Tidy modules
        run: |
          go get \
            github.com/google/uuid@latest \
            github.com/gorilla/websocket@latest \
            github.com/prometheus/client_golang@v1.23.2 \
            github.com/rabbitmq/amqp091-go@latest \
            github.com/redis/go-redis/v9@latest
          go mod tidy
      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@v0.5.1
      - name: Lint (go vet)
        run: go vet ./...
      - name: Lint (staticcheck)
        run: staticcheck ./...
      - name: Test
        run: go test ./... -v -race -coverprofile=coverage.out
      - name: Build (verify compilation)
        run: go build ./...

  build-and-push:
    name: Build & Push Docker
    runs-on: ubuntu-latest
    needs: lint-and-test
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: services/subscription
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
