{
  "name": "onchain-health-monitor",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "markdownlint-cli": "^0.44.0"
  },
  "lint-staged": {
    "*.md": "markdownlint --ignore node_modules --ignore vendor"
  }
}
