apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "api.fullname" . }}-config
  labels:
    {{- include "api.labels" . | nindent 4 }}
data:
  SERVICE_PORT: "8080"
  SERVICE_NAME: "api"
  LOG_LEVEL: "info"
