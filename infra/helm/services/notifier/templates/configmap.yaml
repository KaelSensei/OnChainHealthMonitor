apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "notifier.fullname" . }}-config
  labels:
    {{- include "notifier.labels" . | nindent 4 }}
data:
  SERVICE_PORT: "8083"
  SERVICE_NAME: "notifier"
  LOG_LEVEL: "info"
