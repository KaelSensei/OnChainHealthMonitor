apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "analyzer.fullname" . }}-config
  labels:
    {{- include "analyzer.labels" . | nindent 4 }}
data:
  SERVICE_PORT: "8082"
  SERVICE_NAME: "analyzer"
  LOG_LEVEL: "info"
